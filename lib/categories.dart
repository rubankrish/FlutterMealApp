import 'package:flutter/material.dart';

class Categories {
  final String id;
  final String title;
  final Color color;
  final IconData icon;
  const Categories(
      {@required this.id, @required this.title, this.color, this.icon});
}
